//const clientsSlice = createSlice({
//  name: 'clients',
//  initialState,
//  reducers: {},
//  extraReducers: (builder) => {
//    builder.addMatcher(apiSlice.endpoints.getClients.matchFulfilled, (state, { payload }) => {
//      clientsAdapter.setAll(state, payload);
//    });
//  }
//});
//
//export const {selectAll}
