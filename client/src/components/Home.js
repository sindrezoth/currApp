import { useNavigate, Link, useLocation } from 'react-router-dom';
import RegisterForm from '../features/auth/RegisterForm.js';
import SignUp from '../apps/public/SignUp'

const Home = () => {
  return (
    <SignUp />
  );
}

export default Home;
